[project]
name = "prk-core"
version = "0.1.0"
description = "Core evaluation engine for Prompt Regression Kit"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Eren Ozturk", email = "erenozturk@gmail.com" }
]
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/prk_core"]